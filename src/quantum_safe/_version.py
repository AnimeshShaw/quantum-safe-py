# This file is the single source of truth for the package version.
# pyproject.toml reads from here via hatch-vcs in release builds;
# during development we just hardcode it.
__version__ = "0.1.0"
__version_info__ = (0, 1, 0)
